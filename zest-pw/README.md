# Playwright Test Project

Проект для автоматизованого тестування з автоматичними скріншотами після кожного кроку.

## 🚀 Запуск тестів

### Базовий запуск (без збереження скріншотів на диск)
```bash
npm test
# або
npx playwright test
```

### Запуск зі збереженням скріншотів на диск
```bash
npm run test:screenshots
```

### Запуск зі скріншотами та відкритим браузером
```bash
npm run test:screenshots:headed
```

## 📸 Скріншоти

Скріншоти автоматично створюються після кожного кроку тесту та зберігаються:
- **В пам'яті** (за замовчуванням) - доступні в звіті тестів як base64
- **На диску** (опціонально) - якщо встановлена змінна `SAVE_SCREENSHOTS=true`

### Де знайти скріншоти?

**Після запуску `npm run test:screenshots`:**
- Папка: `screenshots/`
- Формат назви: `{назва_тесту}__{назва_кроку}__{timestamp}.png`

## 📊 Перегляд результатів

Після запуску тестів ви побачите детальний звіт з інформацією про:
- Статус кожного тесту
- Кроки тесту (виконані та не виконані)
- Скріншоти для кожного кроку (base64)
- Помилки (якщо є)

## 🛠️ Структура проекту

```
test-playwright/
├── tests/              # Тестові файли
├── zest-pw/           # Кастомна інфраструктура
│   ├── fixtures/      # Розширені fixtures з автоскріншотами
│   ├── reporter/      # Кастомний репортер
│   └── utils/         # Утиліти (скріншоти, форматування)
├── scripts/           # Допоміжні скрипти
└── screenshots/       # Скріншоти (створюється автоматично)
```

## 📝 Приклад тесту

```typescript
import { test, expect } from '../zest-pw/fixtures/fixtures'

test('TC-001: Check the title', async ({ page }) => {
  
  await test.step('Go to the playwright website', async () => {
    await page.goto('https://playwright.dev/');
  });

  await test.step('Check the title', async () => {
    await expect(page).toHaveTitle(/Playwright/);
  });
});
```

Скріншоти автоматично створюються після кожного `test.step()`.

## 💡 Корисні команди

```bash
# Запустити конкретний тест
npm test -- tests/example.spec.ts

# Запустити тести в debug режимі
npm test -- --debug

# Запустити тести з UI
npm test -- --ui

# Переглянути звіт
npm run print:report
```

